name: Build and Push Docker Images

on:
  push:
    branches:
      - main
    paths:
      - 'ros2_base_images/**'
      - 'ros2_mujoco/**'
      - '.github/workflows/docker-build-push.yml'
  workflow_dispatch:  # 允许手动触发

env:
  DOCKER_HUB_REPO: yutouxiaozhuan/docker_images

jobs:
  # ======== Job 1: 构建并推送 ros2_humble 基础镜像 ========
  build-base:
    runs-on: ubuntu-latest
    # 仅在 ros2_base_images 目录或工作流文件有变更时运行
    if: |
      contains(github.event.head_commit.modified, 'ros2_base_images/') ||
      contains(github.event.head_commit.modified, '.github/workflows/') ||
      github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push ros2_humble
        uses: docker/build-push-action@v5
        with:
          context: ./ros2_base_images
          file: ./ros2_base_images/Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_HUB_REPO }}:ros2_humble
            ${{ env.DOCKER_HUB_REPO }}:ros2_humble-${{ github.run_number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ======== Job 2: 构建并推送 ros2_humble_mujoco 镜像（依赖 Job 1） ========
  build-mujoco:
    needs: build-base
    runs-on: ubuntu-latest
    # 如果基础镜像构建成功，或者仅 mujoco 目录有变更，则构建
    if: |
      needs.build-base.result == 'success' ||
      success()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push ros2_humble_mujoco
        uses: docker/build-push-action@v5
        with:
          context: ./ros2_mujoco
          file: ./ros2_mujoco/Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_HUB_REPO }}:ros2_humble_mujoco
            ${{ env.DOCKER_HUB_REPO }}:ros2_humble_mujoco-${{ github.run_number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max